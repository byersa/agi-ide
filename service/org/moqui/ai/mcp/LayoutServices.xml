<services xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/service-3.0.xsd">

    <!-- 🎯 Change the path name right here -->
    <service verb="modify" noun="Blueprint" type="inline" 
             name="org.moqui.ai.mcp.LayoutServices.modify#Blueprint">
        <description>Modify a blueprint layout tree on the server</description>
        <in-parameters>
            <parameter name="artifactPath" type="String" required="true"/>
            <parameter name="metaJsonData" type="String" required="true"/>
        </in-parameters>
        <actions>
            <script><![CDATA[
                ec.service.sync().name("org.moqui.ide.BlueprintServices.store#Blueprint")
                    .parameters([artifactPath: artifactPath, metaJsonData: metaJsonData])
                    .call()
            ]]></script>
        </actions>
    </service>

    <service verb="add" noun="FormField">
        <description>Use this tool when the user requests to add, create, or insert a new input field, form element, or data column into the active user interface layout canvas. Do not attempt to write text commands; execute this tool with the appropriate node target parameters to modify the layout structure directly.</description>
        <in-parameters>
            <parameter name="targetNodeId" required="true">
                <description>The unique ID string of the parent container element, section, or form panel inside the active layout tree where this new field should be appended.</description>
            </parameter>
            <parameter name="label" required="true">
                <description>The human-readable label or title text that will display next to the field in the user interface (e.g., 'Medical History').</description>
            </parameter>
            <parameter name="encrypt" type="String" default-value="false">
                <description>MUST be set to 'true' if the field captures sensitive data, medical history, or Personally Identifiable Information (PII) to comply with HIPAA guidelines.</description>
            </parameter>
        </in-parameters>
        <out-parameters>
            <parameter name="status" type="String"/>
        </out-parameters>
        <actions>
        <!-- Inside LayoutServices.xml -> add#FormField -> actions -->
        <script><![CDATA[
            import groovy.json.JsonSlurper
            import groovy.json.JsonOutput
        
            ec.logger.info("🚀 [TOOL EXECUTION] add#FormField called with: targetNodeId=${targetNodeId}, label=${label}, encrypt=${encrypt}")
            boolean secureField = "true".equalsIgnoreCase(encrypt)
            String cleanName = label.replaceAll("\\s+", "")
        
            // 🎯 LAYER RECOVERY STEP
            // Look in the direct service parameters first, fall back to execution context memory
            String currentMetaJsonStr = context.activeLayoutBuffer ?: ec.context.activeLayoutBuffer
        
            // 🛡️ REMEDIATION CRITICAL FALLBACK:
            // If thread contexts are isolated, load the record fresh from the DB using the transaction context ID pointers
            if (!currentMetaJsonStr || currentMetaJsonStr.trim() == "") {
                String bufferId = context.activeLayoutBufferId ?: ec.context.activeLayoutBufferId
                ec.logger.info("ℹ️ [add#FormField] activeLayoutBuffer context empty. Attempting DB recovery via ID: ${bufferId}")
                
                if (bufferId) {
                    def bufferRow = ec.entity.find("org.moqui.ai.WorkspaceBuffer").condition("workspaceBufferId", bufferId).one()
                    currentMetaJsonStr = bufferRow?.metaJsonBuffer
                    ec.logger.info("🎯 [add#FormField] Successfully restored tree layout from database cache row.")
                }
            }
        
            // Ultimate pristine fallback if both layers miss
            if (!currentMetaJsonStr) {
                currentMetaJsonStr = "{ \"screen\": \"SampleForm\", \"mariaId\": \"SampleForm#root\", \"widgets\": [] }"
            }
        
            ec.logger.info("[add#FormField] Working Meta-JSON Content: ${currentMetaJsonStr}")
            Map layoutTree = new JsonSlurper().parseText(currentMetaJsonStr)
        
            // Build out the target child layout descriptor
            Map dynamicFieldNode = [
                id: java.util.UUID.randomUUID().toString(),
                type: "FormField",
                name: cleanName,
                title: label,
                mariaId: "component://agi-ide/screen/SampleForm.xml#" + cleanName,
                children: []
            ]
        
            // Call the decoupled tree mutation engine
            Map mutationResult = ec.service.sync().name("org.moqui.ai.mcp.LayoutServices.mutate#JsonTree")
                .parameters([layoutTree: layoutTree, targetNodeId: targetNodeId, action: "APPEND", payloadNode: dynamicFieldNode])
                .call()
        
            if (mutationResult.mutated) {
                ec.logger.info("🌲 [TOOL MUTATION SUCCESS] Node surgically injected under parent: ${targetNodeId}")
                String updatedMetaJson = JsonOutput.toJson(layoutTree)
                
                // 🎯 WRITE-BACK RESYNC: Update the database record instantly so the child editors fetch fresh state
                String bufferId = context.activeLayoutBufferId ?: ec.context.activeLayoutBufferId
                if (bufferId) {
                    ec.service.sync().name("org.moqui.ai.AgiWorkspaceServices.store#WorkspaceBuffer")
                        .parameters([workspaceBufferId: bufferId, metaJsonBuffer: updatedMetaJson])
                        .call()
                }
        
                String jsonPacket = groovy.json.JsonOutput.toJson([
                    event: "artifact-state-mutated",
                    mutatedTree: layoutTree
                ])
                
                ec.makeNotificationMessage()
                    .topic("agi-ide-workspace")
                    .message(jsonPacket)
                    .send()
                context.status = "SUCCESS"
            } else {
                ec.logger.warn("❌ [TOOL MUTATION FAILED] Target node container [${targetNodeId}] was NOT found in the active tree buffer.")
                context.status = "NODE_NOT_FOUND"
            }
        ]]></script>
        </actions>
    </service>

    <service verb="mutate" noun="JsonTree" type="inline">
        <in-parameters>
            <parameter name="layoutTree" type="Map" required="true"/>
            <parameter name="targetNodeId" type="String" required="true"/>
            <parameter name="action" type="String" required="true"/>
            <parameter name="payloadNode" type="Map" required="false"/>
        </in-parameters>
        <out-parameters>
            <parameter name="mutated" type="Boolean"/>
        </out-parameters>
        <actions>
            <script><![CDATA[
                ec.logger.info("[TOOL MUTATION] layoutTree: ${layoutTree}")
                ec.logger.info("[TOOL MUTATION] targetNodeId: ${targetNodeId}")
                ec.logger.info("[TOOL MUTATION] action: ${action}")
                ec.logger.info("[TOOL MUTATION] payloadNode: ${payloadNode}")
                def processNode
                processNode = { Map node ->
                    ec.logger.info("[TOOL MUTATION] node(1): ${node}")
                    // 🎯 ROBUST ROOT/NODE ID MATCHING
                    // Checks if node.id matches, node.mariaId matches exactly, or ends with the target ID suffix (#root)
                    boolean isTarget = (node.id == targetNodeId) || 
                                       (node.mariaId == targetNodeId) || 
                                       (node.mariaId?.toString()?.endsWith("#" + targetNodeId))

                    ec.logger.info("[TOOL MUTATION] isTarget: ${isTarget}")
                    if (isTarget) {
                        switch (action) {
                            case "APPEND":
                                // Adapt gracefully to either a "widgets" structure or a standard "children" layout array
                                if (node.widgets != null) {
                                    node.widgets.add(payloadNode)
                                } else {
                                    if (node.children == null) node.children = []
                                    node.children.add(payloadNode)
                                }
                                return true
                            case "REPLACE":
                                node.clear()
                                node.putAll(payloadNode)
                                return true
                        }
                    }
                    ec.logger.info("[TOOL MUTATION] node(2): ${node}")
                    
                    // Recursive array scanning block supporting both design idioms
                    def childList = node.widgets ?: node.children
                    ec.logger.info("[TOOL MUTATION] childList: ${childList}")
                    if (childList && childList instanceof List) {
                        if (action == "DELETE") {
                            boolean removed = childList.removeIf { 
                                it.id == targetNodeId || it.mariaId == targetNodeId || it.mariaId?.toString()?.endsWith("#" + targetNodeId)
                            }
                            if (removed) return true
                        }
                        for (child in childList) {
                            if (child instanceof Map && processNode(child as Map)) return true
                        }
                    }
                    return false
                }
                context.mutated = processNode(layoutTree)
            ]]></script>
        </actions>
    </service>
</services>