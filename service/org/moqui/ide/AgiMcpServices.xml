<?xml version="1.0" encoding="UTF-8"?>
<services xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/service-3.0.xsd">

    <service verb="connect" noun="McpBridgeConnection" type="inline">
        <actions>
            <script location="component://agi-ide/service/org/moqui/ide/McpBridgeConnection.groovy"/>
        </actions>
    </service>

    <service verb="get" noun="XmlArtifactBlueprint" read-only="true">
        <in-parameters>
            <parameter name="targetComponent" required="true"/>
            <parameter name="artifactPath" required="true"/>
        </in-parameters>
        <out-parameters>
            <parameter name="blueprintJson" type="Map"/>
        </out-parameters>
        <actions>
            <script><![CDATA[
                // Constructs an isolated, mock execution context to force a qjson compile pass
                // through your live DeterministicVueRenderer class.
                String screenLocation = "component://${targetComponent}/${artifactPath}"
                
                try {
                    // Leverages Moqui's native ScreenFacade to build a render pass programmatically
                    def renderComm = ec.screen.makeRender()
                        .withScreen(screenLocation)
                        .renderMode("qjson") // Tells ScreenRenderImpl to activate our custom renderer!
                    
                    StringWriter writer = new StringWriter()
                    renderComm.withWriter(writer).render()
                    
                    // Slurp the generated JSON tree straight back into an operational Map object
                    def jsonMap = new groovy.json.JsonSlurper().parseText(writer.toString())
                    context.blueprintJson = jsonMap
                } catch (Exception e) {
                    ec.logger.error("❌ [AGI-IDE SERVICE ERROR] Failed to serialize artifact tree: " + e.getMessage(), e)
                    context.blueprintJson = [name: "Error", attributes: [text: e.getMessage()]]
                }
            ]]></script>
        </actions>
    </service>

    <service verb="get" noun="AllTools" type="script" location="component://agi-ide/script/getAllTools.groovy">
        <out-parameters>
            <parameter name="toolsList" type="List"/>
        </out-parameters>
    </service>

    <service verb="execute" noun="AdkProxyLoop"  name="executeAdkProxyLoop" type="script" 
        location="component://agi-ide/script/executeAdkProxyLoop.groovy" read-only="true">
        <in-parameters>
            <parameter name="userPrompt" required="true" type="String"/>
            <parameter name="activeTree" required="false" type="Map"/>
            <parameter name="focusCoordinate" required="false" type="String"/>
        </in-parameters>
        <out-parameters>
            <parameter name="completionText" required="true" type="String"/>
        </out-parameters>
    </service>

    <service verb="get" noun="FormMetadata" type="inline">
        <description>
            Compiles a raw Moqui XML screen form artifact path into a flattened Meta-JSON schema definition string.
        </description>
        <in-parameters>
            <parameter name="screenPath" type="String" required="true"/>
        </in-parameters>
        <out-parameters>
            <parameter name="metadataJson" type="String"/>
        </out-parameters>
        <actions>
            <log message="In get#FormMetadata, screenPath: ${screenPath}" />
            <script><![CDATA[
                def targetForm = screenPath
                def componentName = "agi-ide"

//                if (targetForm.contains("/")) {
//                    def pathParts = targetForm.split("/")
//                    componentName = pathParts[0]
//                    targetForm = pathParts[1]
//                }

//                String fileLocation = "component://${componentName}/screen/${componentName}/${targetForm}.xml".toString()
                String fileLocation = screenPath
                ec.logger.info("In getFormMetadata, fileLocation: " + fileLocation);

                try {
                    def resourceRef = ec.resource.getLocationReference(fileLocation)
                    if (!resourceRef || !resourceRef.exists) {
                        fileLocation = "component://${componentName}/screen/${targetForm}.xml".toString()
                        resourceRef = ec.resource.getLocationReference(fileLocation)
                    }

                    if (resourceRef && resourceRef.exists) {
                        // 1. Initialize the base screen renderer context
                        org.moqui.impl.screen.ScreenRenderImpl sri = new org.moqui.impl.screen.ScreenRenderImpl(ec.screen)
                        
                        // 2. Set standard configuration states
                        sri.rootScreen(fileLocation)
                        sri.renderMode("qmeta".toString())
                        
                        // FIXED: Pull the webapp name from the servlet context instead of the web facade property block
                        String resolvedWebappName = ec.web.request.servletContext.getInitParameter("moqui-name")
                        sri.webappName(resolvedWebappName ? resolvedWebappName.toString() : "agi-ide")
                        
                        // 3. Bind a dedicated text stream writer to catch the compiled metadata output
                        java.io.StringWriter sw = new java.io.StringWriter()
                        sri.internalWriter = sw
                        
                        // 4. Force isolated internal parsing structures without running an endless browser loop
                        sri.rootScreenDef = ec.screen.getScreenDefinition(fileLocation)
                        sri.screenUrlInfo = org.moqui.impl.screen.ScreenUrlInfo.getScreenUrlInfo(sri, sri.rootScreenDef, new ArrayList<String>(), null, 0)
                        sri.screenUrlInstance = sri.screenUrlInfo.getInstance(sri, false)
                        
                        // Process the widget nodes straight into our local StringWriter string buffer
                        sri.doActualRender()
                        
                        // 5. Assign the compiled text map down to the out-parameter variable
                        metadataJson = sw.toString()
                    } else {
                        metadataJson = '{"error": "Target layout asset file not found on disk context."}'
                    }
                } catch (Exception ex) {
                    ec.logger.error("Failed to build dynamic component widget metadata tree via isolated rendering pass", ex)
                    metadataJson = '{"error": "Exception occurred during direct screen compilation pass. See server console logs."}'
                }
            ]]></script>
        </actions>
    </service>

</services>