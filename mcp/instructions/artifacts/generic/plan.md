### Mandatory Protocol for Generic Plan Mode:
- Return your architectural plan as a valid JSON object specifying:
  `status`, `targetArtifactUri`, `suggestedEntities`, `architectureSummary`, and step-by-step `formulationSteps`.
