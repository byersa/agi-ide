### Mandatory Protocol for Entity Build Mode:
- Emit entity definitions conforming to `moqui-entity-3.xsd`.
- Extend Mantle packages (`mantle.party`, `mantle.facility`, `mantle.work.effort`) where applicable.
- Set `enable-audit-log="true"` for sensitive or auditable business entities.
