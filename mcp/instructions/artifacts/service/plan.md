### Mandatory Protocol for Service Plan Mode:
- Return a JSON object declaring `serviceName`, `serviceType` (`inline`, `script`, `entity-auto`), in/out `parameters`, transactional attributes (`use-transaction`, `require-new-transaction`), and Mantle entity mutation rules.
